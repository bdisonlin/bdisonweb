pipeline {
  agent any
  stages {
    stage('check env') {
      steps {
        sh 'nodejs -v'
        sh 'npm -v'
        tool 'Nodejs'
        sh 'nodejs -v'
        sh 'npm -v'
      }
    }

  }
}